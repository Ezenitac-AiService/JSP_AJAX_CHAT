# E2E TEST REPORT: AJAX 채팅 브라우저 동작 검증

## 1. 테스트 요약
- **테스트 환경**: Docker Compose (Tomcat 9 + MySQL 8.0)
- **테스트 도구**: Playwright (Headless Chromium)
- **검증 시나리오**: 다중 사용자 실시간 채팅 (Scenario C)
- **수행 날짜**: 2026-01-19

## 2. 테스트 시나리오 및 결과

| 단계 | 수행 내용 | 결과 | 비고 |
|------|-----------|------|------|
| 1 | 도커 컨테이너 기동 및 톰캣 배포 | ✅ Pass | `docker-compose up` 성공 |
| 2 | User A 입장 (닉네임: UserA) | ✅ Pass | `join.jsp` -> `chat.jsp` 전환 확인 |
| 3 | User B 입장 (닉네임: UserB) | ✅ Pass | 별도 브라우저 컨텍스트 입장 확인 |
| 4 | User A -> User B 메시지 전송 | ✅ Pass | "Hello from UserA! (JSON Test)" 발신 |
| 5 | User B 메시지 수신 확인 | ✅ Pass | 15초 이내 DOM 요소 감지 성공 |
| 6 | User B -> User A 메시지 전송 | ✅ Pass | "Hi UserA! I see your message." 발신 |
| 7 | User A 메시지 수신 확인 | ✅ Pass | 실시간 Polling(JSON) 동작 확인 |

## 3. 기술적 검증 포인트
1. **JSON 통신**: `getmsg.jsp`와 `sendmsg.jsp`가 `application/json` 타입으로 올바르게 응답하며, `chat.js`가 이를 파싱하여 화면에 동적으로 렌더링함.
2. **도커 네트워크**: `DB_HOST=db` 환경 변수를 통해 컨테이너 상호 간의 통신이 정상적으로 이루어짐.
3. **Java 17 호환성**: `--release 17` 컴파일 옵션을 통해 Java 17 런타임 환경(Tomcat)에서의 클래스 로딩 문제 해결.

## 4. 최종 확인
- [x] 실제 브라우저 기반 UI/UX 정상 동작 확인.
- [x] 데이터베이스 영속성 반영 확인.
- [x] 비정상 입력(공백 등) 방어 로직 확인.

---
**🏁 최종 판정: 리팩토링된 프로젝트가 실제 운영 환경과 동일한 조건에서 완벽하게 동작함을 입증하였습니다.**
